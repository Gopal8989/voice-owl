# VoiceOwl API - Environment Variables Example
# Copy this file to .env and update with your actual values

# ============================================
# Server Configuration
# ============================================
PORT=7070
NODE_ENV=development

# ============================================
# MongoDB Configuration
# ============================================
# Local MongoDB
MONGODB_URI=mongodb://localhost:27017/voiceowl

# MongoDB Atlas (Cloud) - Replace with your connection string
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/voiceowl?retryWrites=true&w=majority

# MongoDB Connection Pool Configuration (Optional)
MONGODB_MAX_POOL_SIZE=10
MONGODB_MIN_POOL_SIZE=2

# ============================================
# Azure Speech Service Configuration (Optional)
# ============================================
# Get your credentials from Azure Portal: https://portal.azure.com
# Navigate to: Azure Cognitive Services > Your Speech Service > Keys and Endpoint
AZURE_SPEECH_KEY=your_azure_speech_key_here
AZURE_SPEECH_REGION=your_azure_speech_region_here
# Example regions: eastus, westus, westeurope, southeastasia, etc.

# Note: If Azure credentials are not configured, the service will use mock transcription

# ============================================
# Retry Configuration (Optional)
# ============================================
# Maximum number of retry attempts for failed requests
MAX_RETRY_ATTEMPTS=3

# Delay between retries in milliseconds
RETRY_DELAY_MS=1000

# ============================================
# Development Notes
# ============================================
# 1. Never commit the actual .env file to version control
# 2. Always use .env.example as a template
# 3. For production, use environment variables set in your hosting platform
# 4. Azure Speech credentials are optional - the API will work with mock transcription if not provided
